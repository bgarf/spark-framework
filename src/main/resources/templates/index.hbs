{{#partial "content"}}
{{#if username}}
    <h1>SHE WAS A QUEEN</h1>
{{else}}
    <h1>Are you SMOLE worthy?</h1>
    <form action="/sign-in" method="post">
        <input type="text" placeholder="What is your name?" name="username">
        <button>Sign in</button>
    </form>
{{/if}}
{{/partial}}

{{> block.hbs}}